require('./browserSync/index.js');

const exec  = require('child_process').exec;

/*exec('~/node_modules/bin/jspm update', (error, stdout, stderr) => {
 console.log('stdout: ' + stdout);
 console.log('stderr: ' + stderr);
 if (error !== null) {
 console.log('exec error: ' + error);
 }
 });*/
